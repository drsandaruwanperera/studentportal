const form=document.getElementById('accessForm');const code=document.getElementById('code');const msg=document.getElementById('msg');const demo=document.getElementById('demoBtn');demo?.addEventListener('click',()=>{code.value='ACC-2026-001';form.requestSubmit()});form?.addEventListener('submit',e=>{e.preventDefault();const value=code.value.trim().toUpperCase();if(value==='ACC-2026-001'){msg.textContent='Demo access accepted. Login integration will be connected to your student database.';msg.style.color='#1769e0';setTimeout(()=>location.href='login.html?book=accounting',700)}else{msg.textContent='Invalid or expired book access code.';msg.style.color='#d14343'}});document.addEventListener('contextmenu',e=>{if(e.target.closest('.book-card'))e.preventDefault()});
